import { TopicDropDownFilterPipe } from './topic-drop-down-filter.pipe';

describe('TopicDropDownFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicDropDownFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
